#include <stdio.h>
#include <math.h>
// #include <stdbool.h>

int main() {
    /*
    // Challenge 1: Affichage Informations
    char nom[20], prenom[20];
    int age;
    // char sexe[2];
    char sexe;
    char email[50];

    printf("donner le nom: ");  
    scanf("%s", &nom);
    printf("donner le prenom: ");  
    scanf("%s", &prenom);
    printf("donner le age: "); 
    scanf("%d", &age); 
    printf("donner le sexe (M/F): "); 
    scanf(" %c", &sexe); 
    // scanf("%s", &sexe); 
    printf("donner le email: "); 
    scanf("%s", &email); 

    printf("----- Informations Personnelles-----\n");
    printf("le nom et prenom: %s %s\n", prenom, nom);
    printf("Age: %d Ans\n", age);
    printf("sexe: %c\n", sexe);
    // printf("sexe: %s\n", sexe);
    printf("email: %s\n", email);

    // Challenge 2: Conversion de la température
    int C;
    double K;
    printf("Donner la température: ");
    scanf("%d", &C);
    K = C + 273.15;
    printf("la température en Celsius %d et la transforme en Kelvin %.2f", C, K);

    // Challenge 3: Conversion de la distance
    // double kilometres, Yards;
    float kilometres, Yards;
    printf("Donner la distance: ");
    // scanf("%lf", &kilometres);
    scanf("%f", &kilometres);
    Yards = kilometres * 1093.61;
    printf("la distance en kilomètres %.2f et la transforme en yards %.2f", kilometres, Yards);
    
    // Challenge 4: Conversion de la vitesse
    double kilometres_par_heure, metres_par_seconde;
    printf("Donner la vitesse en kilomètres par heure (km/h): ");
    scanf("%lf", &kilometres_par_heure);
    metres_par_seconde = kilometres_par_heure * 0.27778;
    printf("la vitesse en kilomètres par heure %.2f(km/h) et la transforme en mètres par seconde %.2f(m/s).", kilometres_par_heure, metres_par_seconde);
    
    // ChallDenge 5: Affichage Température
    int Celsius;
    printf("Donner la température en Celsius: ");
    scanf("%d", &Celsius);
    if(Celsius < 0) {
        printf("l'état de l'eau à Solide\n");
    } else if(Celsius < 100) {
        printf("l'état de l'eau à Liquide\n");
    } else {
        printf("l'état de l'eau à Gaz\n");
    }
    
    // Challenge 6 : Calcul et affichage des résultats
    float nombre1, nombre2;
    printf("Donner Deux nombres réels a et b: ");
    scanf("%f%f", &nombre1, &nombre2);
    printf("%.2f + %.2f = %.2f\n", nombre1, nombre2, nombre1 + nombre2);
    printf("%.2f - %.2f = %.2f\n", nombre1, nombre2, nombre1 - nombre2);
    printf("%.2f * %.2f = %.2f\n", nombre1, nombre2, nombre1 * nombre2);
    printf("%.2f / %.2f = %.2f\n", nombre1, nombre2, nombre1 / nombre2);
    
    // Challenge 7 : Moyenne pondérée de trois nombres
    float nombre1, nombre2, nombre3, moyenne;
    printf("Donner Trois nombres réels: ");
    scanf("%f%f%f", &nombre1, &nombre2, &nombre3);
    moyenne = (nombre1 * 2 + nombre2 * 3 + nombre3 * 5) / (2 + 3 + 5);
    printf("la moyenne pondérée de trois nombres et: %.2f", moyenne);
    
    // Challenge 8 : Calcul de la moyenne géométrique
    float a, b, c, moyenne_geometrique;
    printf("Donner Trois nombres: ");
    scanf("%f%f%f", &a, &b, &c);
    moyenne_geometrique = pow(a * b * c, 1.0/3.0);
    printf("la moyenne géométrique de trois nombres: %.2f\n", moyenne_geometrique);
    
    // Challenge 9 : Calcul de la distance entre deux points dans un espace 3D
    double x1, x2, y1, y2, z1, z2, Distance;
    Distance = sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2) + pow(z2 - z1, 2));
    printf("la distance entre deux points donnés dans un espace 3D est: %.2f\n", Distance);
    
    // Challenge 10 : Volume d'une sphère
    double rayon, volume;
    const double Pi = 3.14;
    printf("Donner le rayon de la sphère: ");
    scanf("%lf", rayon);
    volume = (4.0/3.0) * Pi * pow(rayon, 3);
    printf("le volume d'une sphère est: %.2f\n", volume);
    
    // Challenge 11 : Surface d'un rectangle
    double longueur, largeur, Surface;
    printf("Entrez la longueur du rectangle : ");
    scanf("%lf", &longueur);
    printf("Entrez la largeur du rectangle : ");
    scanf("%lf", &largeur);
    Surface = longueur * largeur;
    printf("La surface du rectangle est : %.2f\n", Surface);
    
    // Challenge 12 : Nombre entier à quatre chiffres en ordre inverse
    char nombre[4];
    int n, inverse;
    n = 1234;
    printf("un nombre entier à quatre chiffres: ");
    scanf("%s", &nombre);
    printf("si le nombre entier est %s, son inverse est %c%c%c%c\n", nombre, nombre[3], nombre[2], nombre[1], nombre[0]);
    inverse = (n % 10) * 1000 + ((n / 10) % 10) * 100 + ((n / 100) % 10) * 10 + (n / 1000);
    printf("si le nombre entier est %d, son inverse est %d\n", n, inverse);
    
    // Challenge 13 : Affichage des valeurs binaire et hexadécimale équivalentes
    int nombre;
    nombre = 65;
    printf("la nombre %d équivalente en binaire et: ", nombre);
    do {
        if (nombre % 2 == 0) {
            printf("%d", 0);
        } else {
            printf("%d", 1);
        }
        nombre /= 2;
    } while (nombre != 0);

    printf("%x\n", nombre);

    convert decimal to hexa decimal katdar bi array hssan + invers affichage
    do {
        printf("%d", nombre % 16);
        nombre /= 16;
        
    } while (nombre != 0);
    */
    
    
    
    return 0;
}